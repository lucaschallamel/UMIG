# Use the same base image as in the compose file
FROM atlassian/confluence-server:7.19.8

# The custom build is no longer used to install the plugin.
# Instead, a volume mount is used in podman-compose.yml to place the file
# directly into the correct runtime directory.