# Use the same base image as in the compose file
FROM atlassian/confluence-server:7.19.8

# Create the directory for user-installed plugins
RUN mkdir -p /var/atlassian/application-data/confluence/plugins/installed-plugins

# Copy the plugin and set the correct ownership so the 'confluence' user can read it.
# This is the critical step that solves the permission issue.
COPY --chown=confluence:confluence groovyrunner-8.43.0.jar /var/atlassian/application-data/confluence/plugins/installed-plugins/